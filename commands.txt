echo : to display the word given as an argument(positional argument)
cat : will print out the contents of the file in the first positional argument
#! (shebang) : specifies which interpreter to use, followed by the full path of the interpreter 
